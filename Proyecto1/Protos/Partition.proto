syntax = "proto3";

service ServerDataNodeService{
    rpc partitionFile(ClientFile) returns(Response) {}
    
    }
    
    message ClientFile {
        bytes File = 1;
    }
    
    message Response {
        string ResponseMessage = 2;
    }
